#create a loop of 1 - 100
#printed alongside the square number of each
#the list must stop before the square number reaches 200

a = 1
b = a
while a != 101:
    if b >= 200:
        break
    print(a, "\t", b)
    a=a+1
    b=a**2


#from the user input, generate a list from that number down to 0
#print a corresponding list of square numbers

n = int(input("Enter a number: "))
n = (list(range(0, n+1)))
print(n)
for i in n:
  print(i**2, end=" ")




   

